name: Deploy Rules

on:
  push:
    tags:
      - '*-dev.*'
      - '*-stg.*'
      - '*-prod.*'
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set profile based on tag
        run: |
          if [[ ${{ github.event_name }} == 'push' ]]; then
          if [[ ${{ github.ref }} == 'refs/tags/*-dev.*' ]]; then
           echo "spring.profiles.active=dev" > application.yml-dev
           REPO_NAME="alison-jung-dev"
          elif [[ ${{ github.ref }} == 'refs/tags/*-stg.*' ]]; then
           echo "spring.profiles.active=stage" > application.yml-stg
           REPO_NAME="alison-jung-stg"
          elif [[ ${{ github.ref }} == 'refs/tags/*-prod.*' ]]; then
           echo "spring.profiles.active=prod" > application.yml-prod
           REPO_NAME="alison-jung-prod"
          else
           echo "spring.profiles.active=local" > application.yml-local
           REPO_NAME=""
          fi
          fi
          - name: Set profile based on tag - Success
            run: echo "Set profile based on tag step was successful."

      - name: Checkout Repository
        uses: actions/checkout@v2
          - name: Checkout Repository - Success
            run: echo "Checkout Repository step was successful."

      - name: Login to AWS ECR
        run: |
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com
          - name: Login to AWS ECR - Success
            run: echo "Login to AWS ECR step was successful."

      - name: Build the Docker image
        run: docker build -t deploy-build .
          - name: Build the Docker image - Success
            run: echo "Build the Docker image step was successful."

      - name: Tag the Docker image
        run: |
          docker tag deploy-build:latest ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com/alison-jung:latest
          - name: Tag the Docker image - Success
            run: echo "Tag the Docker image step was successful."

      - name: Push image to ECR
        run: |
          if [[ -n $REPO_NAME ]]; then
          docker push ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com/${REPO_NAME}:latest
          fi
          - name: Push image to ECR - Success
            run: echo "Push image to ECR step was successful."
